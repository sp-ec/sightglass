info:
  name: Sync Game Details
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}/gamelist/sync-details?active=true"
  params:
    - name: active
      value: "true"
      type: query
    - name: active
      value: "false"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
