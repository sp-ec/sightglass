info:
  name: Sync Game Titles
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/gamelist/sync-titles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
