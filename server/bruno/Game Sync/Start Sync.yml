info:
  name: Start Sync
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_url}}/sync/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
