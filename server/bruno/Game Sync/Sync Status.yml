info:
  name: Sync Status
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/sync/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
