info:
  name: Sync Status
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/sync/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
