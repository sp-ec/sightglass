info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{api_url}}/auth/login"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "email": "admin@steamscope.local",
        "password": "password123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
