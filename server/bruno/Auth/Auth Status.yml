info:
  name: Auth Status
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/auth/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
