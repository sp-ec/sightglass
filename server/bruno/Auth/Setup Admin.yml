info:
  name: Setup Admin
  type: http
  seq: 2

http:
  method: POST
  url: "{{api_url}}/auth/setup"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "email": "admin@steamscope.local",
        "username": "admin",
        "password": "password123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
