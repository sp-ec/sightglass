info:
  name: Signup
  type: http
  seq: 3

http:
  method: POST
  url: "{{api_url}}/auth/signup"
  auth: inherit
  body:
    type: json
    data: |-
      {
        "email": "player@steamscope.local",
        "username": "player",
        "password": "password123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
