info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{api_url}}/users/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
