info:
  name: Search By Title
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/games"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
