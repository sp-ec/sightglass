info:
  name: Chart Data
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/games/chart?mode=tag&bucket_size=500"
  params:
    - name: mode
      value: tag
      type: query
    - name: bucket_size
      value: "500"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
