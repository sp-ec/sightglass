info:
  name: Chart Data
  type: http
  seq: 3

http:
  method: GET
  url: "{{api_url}}/games/chart?mode=review_count&bucket_size=1000000"
  params:
    - name: mode
      value: review_count
      type: query
    - name: bucket_size
      value: "1000000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
