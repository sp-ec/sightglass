info:
  name: Tag Options
  type: http
  seq: 4

http:
  method: GET
  url: "{{api_url}}/games/tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
